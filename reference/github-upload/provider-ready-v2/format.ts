import { createFileRoute } from "@tanstack/react-router";
import { FileCheck2, ShieldAlert, UserCheck } from "lucide-react";
import { StatePanel } from "@/features/provider-ready/components/StatePanel";
export const Route = createFileRoute("/onboarding-status")({ component: OnboardingStatus });
function OnboardingStatus(){return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-4xl space-y-6"><div><p className="text-sm font-bold uppercase tracking-widest text-emerald-700">Zoryn onboarding</p><h1 className="mt-2 text-3xl font-bold">Complete account-state experience</h1><p className="mt-2 text-slate-600">Use these provider-independent states to map Swan onboarding and compliance outcomes.</p></div><StatePanel icon={<UserCheck/>} title="Identity check required" description="Resume the secure hosted verification process before the account can be opened." action={<button className="rounded-xl bg-slate-950 px-4 py-2 text-sm font-bold text-white">Continue verification</button>}/><StatePanel icon={<FileCheck2/>} title="One document needed" description="Upload proof of address issued within the last three months." action={<button className="rounded-xl border border-slate-300 px-4 py-2 text-sm font-bold">Upload document</button>}/><StatePanel icon={<ShieldAlert/>} title="Application under review" description="No action is needed. We will notify the customer when the regulated partner completes its review."/></div></main>}
